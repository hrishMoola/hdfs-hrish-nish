package edu.usfca.cs.chat;

import java.util.Map;

public class Controller {
    // active nodes map with key being IP/hostname and value being directory path
    private Map<String, String> activeNodes;
    // add File system tree (binary tree?)
    // add routing table



}
